[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
test = "python -m unittest discover -p '*_test.py' -v "

[packages]

[dev-packages]

[requires]
python_version = "3.8"
